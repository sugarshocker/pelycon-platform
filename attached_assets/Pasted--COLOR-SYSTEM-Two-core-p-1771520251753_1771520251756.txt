{
  // ═══════════════════════════════════════════════════════════
  // COLOR SYSTEM
  // Two core palettes (primary + secondary) derived from
  // Pelycon brand guide. Grays + accents for complex UI.
  // ═══════════════════════════════════════════════════════════

  "colors": {

    // ── PRIMARY: Pelycon Orange ──────────────────────────────
    // Used for: CTAs, active nav indicators, primary buttons,
    // hover states, links, focused inputs, chart highlights
    "primary": {
      "50":  "#FFF3E6",   // lightest tint — subtle bg fills (light mode)
      "100": "#FFE0C2",   // light mode hover bg
      "200": "#FFC48A",   // light mode active bg, badge bg
      "300": "#FFA352",   // light borders / outlines
      "400": "#F08A30",   // hover on primary buttons
      "500": "#E77125",   // ★ Brand anchor — Pelycon Orange (#E77125)
      "600": "#C95F1C",   // pressed / active state
      "700": "#A44C16",   // dark mode text-on-surface
      "800": "#7F3A10",   // dark mode subtle accent
      "900": "#5A290B"    // darkest — shadows, underlines
    },

    // ── SECONDARY: Ocean Blue ────────────────────────────────
    // Used for: secondary buttons, info badges, chart series 2,
    // gradient start point, links in dark mode, toggles
    "secondary": {
      "50":  "#E6FAF6",
      "100": "#B3F0E6",
      "200": "#80E6D6",
      "300": "#4DDCC6",
      "400": "#26CEB2",
      "500": "#14A38C",   // ★ Brand anchor — Ocean Blue (#14A38C)
      "600": "#108A76",
      "700": "#0C7060",
      "800": "#08574A",
      "900": "#043D34"
    },

    // ── GRAY: Storm Gray foundation ──────────────────────────
    // Derived from Pelycon Storm Gray (#394442).
    // Used for: backgrounds, surfaces, borders, muted text,
    // dividers, disabled states, sidebar, cards
    "gray": {
      "25":  "#FAFAFA",   // light mode page bg
      "50":  "#F5F5F5",   // light mode card bg
      "100": "#EBEBEB",   // light mode borders
      "200": "#D4D4D4",   // light mode dividers / subtle text
      "300": "#A3A3A3",   // placeholder text
      "400": "#737373",   // muted / caption text
      "500": "#525252",   // secondary body text
      "600": "#394442",   // ★ Brand anchor — Storm Gray (#394442)
      "700": "#2D3635",   // dark mode card surface
      "800": "#1E2625",   // dark mode sidebar / elevated surface
      "900": "#141A19",   // dark mode page bg
      "950": "#0D1110"    // dark mode deepest bg (near-black)
    },

    // ── ACCENT: Sun Yellow ───────────────────────────────────
    // Used for: chart bar highlights, sparkline data, "upgrade"
    // CTAs, star ratings, notification badges, progress bars
    "accent": {
      "50":  "#FFFCE6",
      "100": "#FFF7B3",
      "200": "#FFF280",
      "300": "#FFEC4D",
      "400": "#F5E21A",
      "500": "#EFD41A",   // ★ Brand anchor — Sun Yellow (#EFD41A)
      "600": "#D4BC00",
      "700": "#A89500",
      "800": "#7D6F00",
      "900": "#524800"
    },

    // ── SEMANTIC ACCENTS ─────────────────────────────────────
    // Status colors for badges, alerts, and data states
    "success": {
      "light": "#DCFCE7",  // bg in light mode
      "DEFAULT": "#22C55E", // text / icon
      "dark": "#153B23"     // bg in dark mode
    },
    "error": {
      "light": "#FEE2E2",
      "DEFAULT": "#EF4444",
      "dark": "#3B1515"
    },
    "warning": {
      "light": "#FEF3C7",
      "DEFAULT": "#F59E0B",
      "dark": "#3B2F0A"
    },
    "info": {
      "light": "#E0F2FE",
      "DEFAULT": "#3B82F6",
      "dark": "#152540"
    },

    // ── GRADIENT ─────────────────────────────────────────────
    // Brand gradient from Ocean Blue → Pelycon Orange
    // Used for: hero accents, card borders, progress fills,
    // decorative circles, profile ring, chart gradient fills
    "gradient": {
      "start": "#14A38C",  // Ocean Blue
      "end":   "#E77125",  // Pelycon Orange
      "css":   "linear-gradient(135deg, #14A38C 0%, #E77125 100%)"
    }
  },

  // ═══════════════════════════════════════════════════════════
  // TYPOGRAPHY
  // ═══════════════════════════════════════════════════════════

  "typography": {
    "fontFamily": {
      // Pelycon brand: Rift Bold (headlines), Poppins (body)
      // Rift is Adobe-only; fallback to Inter for UI (closer
      // x-height to Poppins than Rift). Use Poppins for body.
      "display": "'Rift', 'Inter', sans-serif",
      "body":    "'Poppins', 'Inter', sans-serif",
      "mono":    "'JetBrains Mono', 'Fira Code', monospace"
    },
    "scale": {
      // Tailwind-aligned size tokens
      "xs":   "0.75rem",    // 12px — captions, badges, timestamps
      "sm":   "0.875rem",   // 14px — table cells, labels, sidebar nav
      "base": "1rem",       // 16px — body, inputs, buttons
      "lg":   "1.125rem",   // 18px — card titles, subheadings
      "xl":   "1.25rem",    // 20px — section headings
      "2xl":  "1.5rem",     // 24px — page title ("Dashboard")
      "3xl":  "1.875rem",   // 30px — hero metric ($21,219.24)
      "4xl":  "2.25rem"     // 36px — hero balance ($102,175.96)
    },
    "weight": {
      "regular":  400,      // body copy, table data
      "medium":   500,      // labels, nav items, subheadings
      "semibold": 600,      // card headings, button text (Poppins Semibold)
      "bold":     700       // hero numbers, page title (Rift Bold)
    },
    "lineHeight": {
      "tight":  1.25,       // headings, metrics
      "normal": 1.5,        // body text
      "loose":  1.75        // captions, long-form
    }
  },

  // ═══════════════════════════════════════════════════════════
  // SPACING & SIZING
  // ═══════════════════════════════════════════════════════════

  "spacing": {
    "unit": 4,              // base 4px grid
    "scale": {
      "0":  "0px",
      "1":  "4px",
      "2":  "8px",
      "3":  "12px",
      "4":  "16px",         // standard gap between elements
      "5":  "20px",         // card internal padding
      "6":  "24px",         // section gaps, card padding (desktop)
      "8":  "32px",         // gap between card rows
      "10": "40px",         // sidebar section separators
      "12": "48px",         // major section separators
      "16": "64px"          // page-level vertical rhythm
    }
  },

  // ═══════════════════════════════════════════════════════════
  // BORDER RADIUS
  // ═══════════════════════════════════════════════════════════

  "borderRadius": {
    "none": "0px",
    "sm":   "4px",          // inputs, inline badges
    "DEFAULT": "8px",       // buttons, dropdowns
    "md":   "12px",         // cards, modals, popovers
    "lg":   "16px",         // large cards, sidebar panels
    "xl":   "20px",         // virtual card display
    "2xl":  "24px",         // modal wrappers
    "full": "9999px"        // pills, avatars, status dots
  },

  // ═══════════════════════════════════════════════════════════
  // SHADOWS & ELEVATION
  // ═══════════════════════════════════════════════════════════

  "shadows": {
    // Dark mode uses border-based elevation (not box-shadow)
    // Light mode uses subtle box-shadows
    "light": {
      "sm":   "0 1px 2px rgba(57, 68, 66, 0.06)",
      "DEFAULT": "0 1px 3px rgba(57, 68, 66, 0.10), 0 1px 2px rgba(57, 68, 66, 0.06)",
      "md":   "0 4px 6px rgba(57, 68, 66, 0.08), 0 2px 4px rgba(57, 68, 66, 0.05)",
      "lg":   "0 10px 15px rgba(57, 68, 66, 0.08), 0 4px 6px rgba(57, 68, 66, 0.04)",
      "xl":   "0 20px 25px rgba(57, 68, 66, 0.10), 0 8px 10px rgba(57, 68, 66, 0.04)"
    },
    "dark": {
      // Elevation through surface color + 1px border
      "sm":   "0 0 0 1px rgba(255,255,255,0.04)",
      "DEFAULT": "0 0 0 1px rgba(255,255,255,0.06)",
      "md":   "0 0 0 1px rgba(255,255,255,0.08)",
      "lg":   "0 4px 12px rgba(0,0,0,0.40)",
      "xl":   "0 8px 24px rgba(0,0,0,0.50)"
    }
  },

  // ═══════════════════════════════════════════════════════════
  // RESPONSIVE BREAKPOINTS (Tailwind defaults)
  // ═══════════════════════════════════════════════════════════

  "breakpoints": {
    "sm":  "640px",
    "md":  "768px",
    "lg":  "1024px",
    "xl":  "1280px",
    "2xl": "1536px"
  },

  // ═══════════════════════════════════════════════════════════
  // TRANSITIONS
  // ═══════════════════════════════════════════════════════════

  "transitions": {
    "fast":    "150ms ease-in-out",
    "DEFAULT": "200ms ease-in-out",
    "slow":    "300ms ease-in-out",
    "spring":  "500ms cubic-bezier(0.34, 1.56, 0.64, 1)" // playful micro-interactions
  }
}